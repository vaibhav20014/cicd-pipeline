pipeline {
    agent any

    environment {
        SONARQUBE_SERVER = 'Sonar' // Your SonarQube server name from Jenkins configuration
        PROJECT_KEY = 'cicd-pipeline' // Unique project key for your SonarQube project
        SONAR_TOKEN = credentials('Sonar') // Replace with the ID of your Jenkins secret with the Sonar token
    }

    stages {
        stage('Clone Repository') {
            steps {
                git (
                    url: 'https://github.com/vaibhav20014/cicd-pipeline.git',
                    branch: 'main',
                    credentialsId: '951f08f7-7683-42c7-b306-907e00f64375'
                )
            }
        }

        stage('SonarQube Scan') {
            steps {
                script {
                    def scannerHome = tool 'Sonar' // Use the scanner tool you set up
                    withSonarQubeEnv(SONARQUBE_SERVER) {
                        sh """
                            ${scannerHome}/bin/sonar-scanner \
                            -Dsonar.projectKey="${cicd-pipeline}" // Corrected to use PROJECT_KEY variable
                            -Dsonar.sources=. \ // Changed to use current directory for sources
                            -Dsonar.host.url=http://13.235.14.58:9000 \
                            -Dsonar.login="${sqa_44b600f51b780257fe4a0c37a8e0c1c1cc8a476d}" // Corrected variable usage
                        """
                    }
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t nginx-server .'
            }
        }

        stage('Run Docker Container') {
            steps {
                sh 'docker stop nginx-container || true'
                sh 'docker rm nginx-container || true'
                sh 'docker run -d --name nginx-container -p 8000:80 nginx-server'
            }
        }

        stage('Copy Files to Running Container') {
            steps {
                script {
                    def container_id = sh(script: 'docker ps --filter "name=nginx-container" --format "{{.ID}}"', returnStdout: true).trim() // Filter by name
                    if (container_id) {
                        sh "docker cp . ${container_id}:/usr/share/nginx/html"
                    }
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
