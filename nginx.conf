server {
    listen 80;
    server_name localhost;

    # Serve static files and set Signup.html as the default page
    location / {
        root /usr/share/nginx/html;
        index Signup.html;  # Set Signup.html as the default file
        try_files $uri $uri/ /Signup.html;  # Fallback to Signup.html if the file is not found
    }

    # Proxy requests for the API - Registration
    location /register {
        proxy_pass http://api-service.default.svc.cluster.local:3000/register;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy requests for the API - Login
    location /login {
        proxy_pass http://api-service.default.svc.cluster.local:3000/login;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Prevent access to sensitive files
    location ~ /\.(ht|git|env).* {
        deny all;
    }

    # Error pages
    error_page 404 /Signup.html;  # Redirect 404 errors to Signup.html
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }

    # Compression for faster loading
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_proxied any;
    gzip_min_length 1000;

    # Caching for static assets
    location ~* \.(?:ico|css|js|gif|jpe?g|png|woff2?|eot|ttf|svg)$ {
        expires 6M;
        access_log off;
        add_header Cache-Control "public";
    }
}
